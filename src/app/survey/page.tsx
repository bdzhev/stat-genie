/**
 * to do
 * 
 * logic must be - check the local storage or check the db
 * if theres an entry in localstorage with data like
 * { step: 0-4, null
 * 1: [values]
 * 2: ... }
 * look at step and check if the number of values matches the required length
 * if it is then verify step length and redirect to required id
 * if a user tries to go to some id and his localstorage empty or step is different
 * try to redirect
 */